
import './Button.scss';
import React from "react";

export default function Button({ props }) {
    return (
        <button className="button">
            {/* <img src={props.icon} /> */}
            {/* {props.name} */}
        </button>
    );
}
